[project]
name = "duly"
version = "0.0.1"
description = "Auditable document decisioning for regulated workflows"
readme = "README.md"
requires-python = ">=3.11"
license = "Apache-2.0"
dependencies = []

[dependency-groups]
dev = [
    "jsonschema>=4.18",
]

[tool.uv]
# Spec-only for now: nothing importable to build or install. Flip to a uv
# workspace with kernel/ and extractors/ as members once M1 adds real packages.
package = false
